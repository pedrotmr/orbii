# Copy to `.env.local` (gitignored).
# Same Convex URL as packages/backend CONVEX_URL.
EXPO_PUBLIC_CONVEX_URL=
# From `clerk env pull` (apps/mobile) or Clerk Dashboard → API keys.
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=
